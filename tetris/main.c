#include "block.h"
#include <stdio.h>

int main(void) {

    int a, b, c, d;
    scanf_s("%d", &a);
    scanf_s("%d", &b);
    scanf_s("%d", &c);
    scanf_s("%d", &d);

    PrintBlock(a, b, c, d);

    return 0;
}